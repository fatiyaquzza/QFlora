import React from "react";
import { Stack } from "expo-router";

const DetailLayout = () => {
  return (
    <Stack
      screenOptions={{
        headerShown: true,
      }}
    />
  );
};

export default DetailLayout;
